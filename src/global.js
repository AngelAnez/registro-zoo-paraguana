import path from "path"
import {fileURLToPath} from 'url';

const __filename = fileURLToPath(import.meta.url);

export const DIR_APP = path.dirname(__filename)

export const TOKEN_SECRET_KEY = "ParaguanaOsoFrontino"